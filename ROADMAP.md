# Roadmap

A system monitor you can rewind.

<!-- generated by cairn; edit the files in `cairn/items` and run `cairn render` -->

## v0.1 — The rewind works

`##########` 100% · 7 of 7 done · due 2026-10-01

Timeline, scrubbing, process tree, colour that survives a colourblind reader and a monochrome terminal.

### done

- [x] `0005` Documented responsive degradation ladder <sup>feature · p2</sup>
- [x] `0008` Micro-bar in the CPU column <sup>feature · p2</sup>
- [x] `0009` Micro-bar for memory, scaled to the machine <sup>feature · p3</sup>
- [x] `0010` Per-process sparkline from history <sup>feature · p0</sup>
- [x] `0015` The timeline is mostly empty air <sup>bug · p0</sup>
- [x] `0016` Per-core meters render as an undifferentiated blob <sup>bug · p1</sup>
- [x] `0017` The process table header is too heavy <sup>bug · p2</sup>

## v0.2 — Cheap enough to run

`####······` 40% · 2 of 5 done · due 2026-11-01

Collector fast enough for sub-second sampling, and the data-fidelity gaps against atop closed or disclosed.

### backlog

- [ ] `0011` Capture processes that live and die between samples <sup>feature · p0</sup>
- [ ] `0012` Mark sampling gaps in the timeline <sup>feature · p2</sup>
- [ ] `0013` Configurable sample interval and window <sup>feature · p2</sup>

### done

- [x] `0006` Read /proc into a reused buffer <sup>feature · p1</sup>
- [x] `0007` Intern process names <sup>feature · p1</sup>

## v1.0 — Yours to configure

`··········` 0% · 0 of 4 done · due 2027-01-01

Config file, user themes, documented and safe to depend on.

### backlog

- [ ] `0001` Config file at ~/.config/ptop/ptop.conf <sup>feature · p2</sup>
- [ ] `0002` User theme files with partial overrides <sup>feature · p2</sup>
- [ ] `0003` Validate user themes and say why one fails <sup>feature · p1</sup>
- [ ] `0004` Configurable warn and critical thresholds <sup>feature · p3</sup>

## later — Someday

`··········` 0% · 0 of 1 done

Good ideas without a date yet.

### backlog

- [ ] `0014` Persist history across restarts <sup>feature · p3</sup>

